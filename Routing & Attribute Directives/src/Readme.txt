step 1 :
create routes in app.routes.ts
check app.module.ts for details

step 2:
in app.html add 
<router-outlet></router-outlet>

step 3:
in a tags instead of href use routerLink
check app.ts for details
make sure also import in app.ts

to show which is active use RouterLinkActive
check app.ts for details
make sure also import in app.ts


Attribute directive bhi hay isme